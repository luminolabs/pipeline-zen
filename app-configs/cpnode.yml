GOOGLE_APPLICATION_CREDENTIALS: .secrets/gcp_key.json
customer_api_enabled: false  # Disables credit check / assumes end user has enough credits (i.e. over minimum balance)
log_stdout: true
delete_results: true